package decorator;

// Component interface — defines the contract for generating a student report
// Both the base report and decorators implement this interface
public interface StudentReport {
    void generate();
}
